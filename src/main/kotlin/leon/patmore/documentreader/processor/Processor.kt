package leon.patmore.documentreader.processor

import org.springframework.data.mongodb.core.MongoTemplate

class Processor(private val mongoTemplate: MongoTemplate) {

    private fun process() {
    }
}
